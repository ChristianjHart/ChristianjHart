name: portrait

on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: pip install pillow

      - run: python3 scripts/generate_ascii.py assets/Profile.jpeg -o ascii.svg

      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ascii.svg
          git diff --cached --quiet || git commit -m "portrait: regenerate ascii.svg"
          git push
